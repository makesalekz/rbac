// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/makesalekz/rbac/ent/resourceaccess"
	"github.com/makesalekz/rbac/ent/resourcetype"
	"github.com/makesalekz/rbac/ent/role"
)

// ResourceAccess is the model entity for the ResourceAccess schema.
type ResourceAccess struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID int64 `json:"tenant_id,omitempty"`
	// ResourceType holds the value of the "resource_type" field.
	ResourceType *string `json:"resource_type,omitempty"`
	// ResourceID holds the value of the "resource_id" field.
	ResourceID *int64 `json:"resource_id,omitempty"`
	// IdentityID holds the value of the "identity_id" field.
	IdentityID string `json:"identity_id,omitempty"`
	// RoleID holds the value of the "role_id" field.
	RoleID int64 `json:"role_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ResourceAccessQuery when eager-loading is set.
	Edges        ResourceAccessEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ResourceAccessEdges holds the relations/edges for other nodes in the graph.
type ResourceAccessEdges struct {
	// Role holds the value of the role edge.
	Role *Role `json:"role,omitempty"`
	// Type holds the value of the type edge.
	Type *ResourceType `json:"type,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// RoleOrErr returns the Role value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ResourceAccessEdges) RoleOrErr() (*Role, error) {
	if e.Role != nil {
		return e.Role, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: role.Label}
	}
	return nil, &NotLoadedError{edge: "role"}
}

// TypeOrErr returns the Type value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ResourceAccessEdges) TypeOrErr() (*ResourceType, error) {
	if e.Type != nil {
		return e.Type, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: resourcetype.Label}
	}
	return nil, &NotLoadedError{edge: "type"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ResourceAccess) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case resourceaccess.FieldID, resourceaccess.FieldTenantID, resourceaccess.FieldResourceID, resourceaccess.FieldRoleID:
			values[i] = new(sql.NullInt64)
		case resourceaccess.FieldResourceType, resourceaccess.FieldIdentityID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ResourceAccess fields.
func (ra *ResourceAccess) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case resourceaccess.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ra.ID = int64(value.Int64)
		case resourceaccess.FieldTenantID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				ra.TenantID = value.Int64
			}
		case resourceaccess.FieldResourceType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field resource_type", values[i])
			} else if value.Valid {
				ra.ResourceType = new(string)
				*ra.ResourceType = value.String
			}
		case resourceaccess.FieldResourceID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field resource_id", values[i])
			} else if value.Valid {
				ra.ResourceID = new(int64)
				*ra.ResourceID = value.Int64
			}
		case resourceaccess.FieldIdentityID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field identity_id", values[i])
			} else if value.Valid {
				ra.IdentityID = value.String
			}
		case resourceaccess.FieldRoleID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field role_id", values[i])
			} else if value.Valid {
				ra.RoleID = value.Int64
			}
		default:
			ra.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ResourceAccess.
// This includes values selected through modifiers, order, etc.
func (ra *ResourceAccess) Value(name string) (ent.Value, error) {
	return ra.selectValues.Get(name)
}

// QueryRole queries the "role" edge of the ResourceAccess entity.
func (ra *ResourceAccess) QueryRole() *RoleQuery {
	return NewResourceAccessClient(ra.config).QueryRole(ra)
}

// QueryType queries the "type" edge of the ResourceAccess entity.
func (ra *ResourceAccess) QueryType() *ResourceTypeQuery {
	return NewResourceAccessClient(ra.config).QueryType(ra)
}

// Update returns a builder for updating this ResourceAccess.
// Note that you need to call ResourceAccess.Unwrap() before calling this method if this ResourceAccess
// was returned from a transaction, and the transaction was committed or rolled back.
func (ra *ResourceAccess) Update() *ResourceAccessUpdateOne {
	return NewResourceAccessClient(ra.config).UpdateOne(ra)
}

// Unwrap unwraps the ResourceAccess entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ra *ResourceAccess) Unwrap() *ResourceAccess {
	_tx, ok := ra.config.driver.(*txDriver)
	if !ok {
		panic("ent: ResourceAccess is not a transactional entity")
	}
	ra.config.driver = _tx.drv
	return ra
}

// String implements the fmt.Stringer.
func (ra *ResourceAccess) String() string {
	var builder strings.Builder
	builder.WriteString("ResourceAccess(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ra.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", ra.TenantID))
	builder.WriteString(", ")
	if v := ra.ResourceType; v != nil {
		builder.WriteString("resource_type=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := ra.ResourceID; v != nil {
		builder.WriteString("resource_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("identity_id=")
	builder.WriteString(ra.IdentityID)
	builder.WriteString(", ")
	builder.WriteString("role_id=")
	builder.WriteString(fmt.Sprintf("%v", ra.RoleID))
	builder.WriteByte(')')
	return builder.String()
}

// ResourceAccesses is a parsable slice of ResourceAccess.
type ResourceAccesses []*ResourceAccess
