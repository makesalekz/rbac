// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/makesalekz/rbac/ent/resourcetype"
	"github.com/makesalekz/rbac/ent/role"
)

// ResourceTypeCreate is the builder for creating a ResourceType entity.
type ResourceTypeCreate struct {
	config
	mutation *ResourceTypeMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetDescription sets the "description" field.
func (rtc *ResourceTypeCreate) SetDescription(s string) *ResourceTypeCreate {
	rtc.mutation.SetDescription(s)
	return rtc
}

// SetNillableDescription sets the "description" field if the given value is not nil.
func (rtc *ResourceTypeCreate) SetNillableDescription(s *string) *ResourceTypeCreate {
	if s != nil {
		rtc.SetDescription(*s)
	}
	return rtc
}

// SetID sets the "id" field.
func (rtc *ResourceTypeCreate) SetID(s string) *ResourceTypeCreate {
	rtc.mutation.SetID(s)
	return rtc
}

// AddRoleIDs adds the "roles" edge to the Role entity by IDs.
func (rtc *ResourceTypeCreate) AddRoleIDs(ids ...int64) *ResourceTypeCreate {
	rtc.mutation.AddRoleIDs(ids...)
	return rtc
}

// AddRoles adds the "roles" edges to the Role entity.
func (rtc *ResourceTypeCreate) AddRoles(r ...*Role) *ResourceTypeCreate {
	ids := make([]int64, len(r))
	for i := range r {
		ids[i] = r[i].ID
	}
	return rtc.AddRoleIDs(ids...)
}

// Mutation returns the ResourceTypeMutation object of the builder.
func (rtc *ResourceTypeCreate) Mutation() *ResourceTypeMutation {
	return rtc.mutation
}

// Save creates the ResourceType in the database.
func (rtc *ResourceTypeCreate) Save(ctx context.Context) (*ResourceType, error) {
	rtc.defaults()
	return withHooks(ctx, rtc.sqlSave, rtc.mutation, rtc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (rtc *ResourceTypeCreate) SaveX(ctx context.Context) *ResourceType {
	v, err := rtc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (rtc *ResourceTypeCreate) Exec(ctx context.Context) error {
	_, err := rtc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (rtc *ResourceTypeCreate) ExecX(ctx context.Context) {
	if err := rtc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (rtc *ResourceTypeCreate) defaults() {
	if _, ok := rtc.mutation.Description(); !ok {
		v := resourcetype.DefaultDescription
		rtc.mutation.SetDescription(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (rtc *ResourceTypeCreate) check() error {
	return nil
}

func (rtc *ResourceTypeCreate) sqlSave(ctx context.Context) (*ResourceType, error) {
	if err := rtc.check(); err != nil {
		return nil, err
	}
	_node, _spec := rtc.createSpec()
	if err := sqlgraph.CreateNode(ctx, rtc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(string); ok {
			_node.ID = id
		} else {
			return nil, fmt.Errorf("unexpected ResourceType.ID type: %T", _spec.ID.Value)
		}
	}
	rtc.mutation.id = &_node.ID
	rtc.mutation.done = true
	return _node, nil
}

func (rtc *ResourceTypeCreate) createSpec() (*ResourceType, *sqlgraph.CreateSpec) {
	var (
		_node = &ResourceType{config: rtc.config}
		_spec = sqlgraph.NewCreateSpec(resourcetype.Table, sqlgraph.NewFieldSpec(resourcetype.FieldID, field.TypeString))
	)
	_spec.OnConflict = rtc.conflict
	if id, ok := rtc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := rtc.mutation.Description(); ok {
		_spec.SetField(resourcetype.FieldDescription, field.TypeString, value)
		_node.Description = value
	}
	if nodes := rtc.mutation.RolesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   resourcetype.RolesTable,
			Columns: []string{resourcetype.RolesColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(role.FieldID, field.TypeInt64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.ResourceType.Create().
//		SetDescription(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.ResourceTypeUpsert) {
//			SetDescription(v+v).
//		}).
//		Exec(ctx)
func (rtc *ResourceTypeCreate) OnConflict(opts ...sql.ConflictOption) *ResourceTypeUpsertOne {
	rtc.conflict = opts
	return &ResourceTypeUpsertOne{
		create: rtc,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.ResourceType.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (rtc *ResourceTypeCreate) OnConflictColumns(columns ...string) *ResourceTypeUpsertOne {
	rtc.conflict = append(rtc.conflict, sql.ConflictColumns(columns...))
	return &ResourceTypeUpsertOne{
		create: rtc,
	}
}

type (
	// ResourceTypeUpsertOne is the builder for "upsert"-ing
	//  one ResourceType node.
	ResourceTypeUpsertOne struct {
		create *ResourceTypeCreate
	}

	// ResourceTypeUpsert is the "OnConflict" setter.
	ResourceTypeUpsert struct {
		*sql.UpdateSet
	}
)

// SetDescription sets the "description" field.
func (u *ResourceTypeUpsert) SetDescription(v string) *ResourceTypeUpsert {
	u.Set(resourcetype.FieldDescription, v)
	return u
}

// UpdateDescription sets the "description" field to the value that was provided on create.
func (u *ResourceTypeUpsert) UpdateDescription() *ResourceTypeUpsert {
	u.SetExcluded(resourcetype.FieldDescription)
	return u
}

// ClearDescription clears the value of the "description" field.
func (u *ResourceTypeUpsert) ClearDescription() *ResourceTypeUpsert {
	u.SetNull(resourcetype.FieldDescription)
	return u
}

// UpdateNewValues updates the mutable fields using the new values that were set on create except the ID field.
// Using this option is equivalent to using:
//
//	client.ResourceType.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(resourcetype.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *ResourceTypeUpsertOne) UpdateNewValues() *ResourceTypeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		if _, exists := u.create.mutation.ID(); exists {
			s.SetIgnore(resourcetype.FieldID)
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.ResourceType.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *ResourceTypeUpsertOne) Ignore() *ResourceTypeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *ResourceTypeUpsertOne) DoNothing() *ResourceTypeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the ResourceTypeCreate.OnConflict
// documentation for more info.
func (u *ResourceTypeUpsertOne) Update(set func(*ResourceTypeUpsert)) *ResourceTypeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&ResourceTypeUpsert{UpdateSet: update})
	}))
	return u
}

// SetDescription sets the "description" field.
func (u *ResourceTypeUpsertOne) SetDescription(v string) *ResourceTypeUpsertOne {
	return u.Update(func(s *ResourceTypeUpsert) {
		s.SetDescription(v)
	})
}

// UpdateDescription sets the "description" field to the value that was provided on create.
func (u *ResourceTypeUpsertOne) UpdateDescription() *ResourceTypeUpsertOne {
	return u.Update(func(s *ResourceTypeUpsert) {
		s.UpdateDescription()
	})
}

// ClearDescription clears the value of the "description" field.
func (u *ResourceTypeUpsertOne) ClearDescription() *ResourceTypeUpsertOne {
	return u.Update(func(s *ResourceTypeUpsert) {
		s.ClearDescription()
	})
}

// Exec executes the query.
func (u *ResourceTypeUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for ResourceTypeCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *ResourceTypeUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *ResourceTypeUpsertOne) ID(ctx context.Context) (id string, err error) {
	if u.create.driver.Dialect() == dialect.MySQL {
		// In case of "ON CONFLICT", there is no way to get back non-numeric ID
		// fields from the database since MySQL does not support the RETURNING clause.
		return id, errors.New("ent: ResourceTypeUpsertOne.ID is not supported by MySQL driver. Use ResourceTypeUpsertOne.Exec instead")
	}
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *ResourceTypeUpsertOne) IDX(ctx context.Context) string {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// ResourceTypeCreateBulk is the builder for creating many ResourceType entities in bulk.
type ResourceTypeCreateBulk struct {
	config
	err      error
	builders []*ResourceTypeCreate
	conflict []sql.ConflictOption
}

// Save creates the ResourceType entities in the database.
func (rtcb *ResourceTypeCreateBulk) Save(ctx context.Context) ([]*ResourceType, error) {
	if rtcb.err != nil {
		return nil, rtcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(rtcb.builders))
	nodes := make([]*ResourceType, len(rtcb.builders))
	mutators := make([]Mutator, len(rtcb.builders))
	for i := range rtcb.builders {
		func(i int, root context.Context) {
			builder := rtcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ResourceTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, rtcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = rtcb.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, rtcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, rtcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (rtcb *ResourceTypeCreateBulk) SaveX(ctx context.Context) []*ResourceType {
	v, err := rtcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (rtcb *ResourceTypeCreateBulk) Exec(ctx context.Context) error {
	_, err := rtcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (rtcb *ResourceTypeCreateBulk) ExecX(ctx context.Context) {
	if err := rtcb.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.ResourceType.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.ResourceTypeUpsert) {
//			SetDescription(v+v).
//		}).
//		Exec(ctx)
func (rtcb *ResourceTypeCreateBulk) OnConflict(opts ...sql.ConflictOption) *ResourceTypeUpsertBulk {
	rtcb.conflict = opts
	return &ResourceTypeUpsertBulk{
		create: rtcb,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.ResourceType.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (rtcb *ResourceTypeCreateBulk) OnConflictColumns(columns ...string) *ResourceTypeUpsertBulk {
	rtcb.conflict = append(rtcb.conflict, sql.ConflictColumns(columns...))
	return &ResourceTypeUpsertBulk{
		create: rtcb,
	}
}

// ResourceTypeUpsertBulk is the builder for "upsert"-ing
// a bulk of ResourceType nodes.
type ResourceTypeUpsertBulk struct {
	create *ResourceTypeCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.ResourceType.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(resourcetype.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *ResourceTypeUpsertBulk) UpdateNewValues() *ResourceTypeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		for _, b := range u.create.builders {
			if _, exists := b.mutation.ID(); exists {
				s.SetIgnore(resourcetype.FieldID)
			}
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.ResourceType.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *ResourceTypeUpsertBulk) Ignore() *ResourceTypeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *ResourceTypeUpsertBulk) DoNothing() *ResourceTypeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the ResourceTypeCreateBulk.OnConflict
// documentation for more info.
func (u *ResourceTypeUpsertBulk) Update(set func(*ResourceTypeUpsert)) *ResourceTypeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&ResourceTypeUpsert{UpdateSet: update})
	}))
	return u
}

// SetDescription sets the "description" field.
func (u *ResourceTypeUpsertBulk) SetDescription(v string) *ResourceTypeUpsertBulk {
	return u.Update(func(s *ResourceTypeUpsert) {
		s.SetDescription(v)
	})
}

// UpdateDescription sets the "description" field to the value that was provided on create.
func (u *ResourceTypeUpsertBulk) UpdateDescription() *ResourceTypeUpsertBulk {
	return u.Update(func(s *ResourceTypeUpsert) {
		s.UpdateDescription()
	})
}

// ClearDescription clears the value of the "description" field.
func (u *ResourceTypeUpsertBulk) ClearDescription() *ResourceTypeUpsertBulk {
	return u.Update(func(s *ResourceTypeUpsert) {
		s.ClearDescription()
	})
}

// Exec executes the query.
func (u *ResourceTypeUpsertBulk) Exec(ctx context.Context) error {
	if u.create.err != nil {
		return u.create.err
	}
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the ResourceTypeCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for ResourceTypeCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *ResourceTypeUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
