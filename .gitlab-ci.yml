include:
  - project: 'deploy/dev'
    file: '/go-app.yml'
    ref: 'main'
  
test dev:
  extends: .test-dev

build dev:
  extends: .build-dev

deploy dev:
  extends: .deploy-dev
  variables:
    DB_PORT: 54409
  
build:
  extends: .build

migration-build:
  extends: .migration-build

helm-lint:
  extends: .helm-lint

deploy:
  extends: .deploy
